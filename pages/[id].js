const CardDetails = ({ id }) => {
  return (
    <div>
      <h1>Card Details</h1>
      <p>Card ID: {id}</p>
      {/* Add more details about the card */}
    </div>
  );
};

export default CardDetails;
